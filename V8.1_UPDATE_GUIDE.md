# HsLab Dashboard V8.1 업데이트

## 수정 내용
- 누락되던 TREND 03 `바이오·의료 VC 투자` 차트 추가
- 누락되던 TREND 05 `바이오 등록특허` 차트 추가
- 6개 차트를 3열 × 2행으로 고정
- 기존 `data.js`가 브라우저에 캐시된 경우에도 두 차트가 표시되도록 안전한 fallback 데이터 추가
- 정적 파일 버전을 `8.1.0`으로 변경해 GitHub Pages 캐시 갱신

## 적용 방법
저장소 최상위에 전체 파일을 덮어쓴 뒤 커밋·푸시하세요. 배포 후에는 한 번 `Ctrl + F5`를 눌러 확인하세요.
